--- net/ipv6/udp.c	2003-11-28 18:26:21.000000000 +0000
+++ net/ipv6/udp.c	2004-01-06 12:45:53.000000000 +0000
@@ -565,6 +565,7 @@
 			if(!ipv6_addr_any(&np->rcv_saddr)) {
 				if(ipv6_addr_cmp(&np->rcv_saddr, loc_addr) == 0)
 					return s;
+				continue;
 			}
 			if(!inet6_mc_check(s, loc_addr, rmt_addr))
 				continue;
